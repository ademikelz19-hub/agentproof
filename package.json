{
  "name": "agentproof",
  "private": true,
  "version": "0.1.0",
  "description": "Open reliability infrastructure for autonomous onchain agents (ERC-8004 / BSC).",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10",
    "drizzle-orm": "^0.45.2",
    "pg": "^8.23.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
